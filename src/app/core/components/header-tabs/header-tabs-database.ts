export class TabTesting {
    id: string;
    title: string;
    url: string;
    icon: string;
    filterData: {};
    filterGroupData: {};
    constructor(id: string, title: string)
    {
        this.id = id;
        this.title = title;
    }
}
