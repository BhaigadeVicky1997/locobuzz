import { HoverDropdownDirective } from './hover-dropdown.directive';

describe('HoverDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
