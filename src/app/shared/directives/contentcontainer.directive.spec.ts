import { ContentcontainerDirective } from './contentcontainer.directive';

describe('ContentcontainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentcontainerDirective();
    expect(directive).toBeTruthy();
  });
});
