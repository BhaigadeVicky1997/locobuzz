import { ResponsiveAdaptDirective } from './responsive-adapt.directive';

describe('ResponsiveAdaptDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveAdaptDirective();
    expect(directive).toBeTruthy();
  });
});
