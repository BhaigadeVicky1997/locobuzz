import { LocobuzzPaginatorDirective } from './locobuzz-paginator.directive';

describe('LocobuzzPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LocobuzzPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
