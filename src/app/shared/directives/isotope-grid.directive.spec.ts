import { IsotopeGridDirective } from './isotope-grid.directive';

describe('IsotopeGridDirective', () => {
  it('should create an instance', () => {
    const directive = new IsotopeGridDirective();
    expect(directive).toBeTruthy();
  });
});
